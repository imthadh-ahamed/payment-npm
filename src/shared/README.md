# shared

Cross-cutting building blocks with no business meaning of their own:
generic result/either types, base error classes, small pure helpers used by
more than one layer.

Kept deliberately small. If something belongs to a single layer, it lives in
that layer instead of here.

No implementation exists yet. This file is a placeholder for Phase 1.
