# utils

Small, pure, dependency-free utility functions used across layers (e.g.
assertion helpers, string/number formatting). Utilities must not encode
business rules — those belong in `domain` or `application`.

No implementation exists yet. This file is a placeholder for Phase 1.
