# config

Configuration schemas and validation for consumers of this package (e.g.
provider credentials, platform-level options). Defines shape and validation
only — it does not read environment variables itself, since this is a
library, not an application.

No implementation exists yet. This file is a placeholder for Phase 1.
