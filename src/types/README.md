# types

Shared TypeScript types and type-only utilities that don't belong to a
single layer (branded types, utility types, ambient type augmentations).

No implementation exists yet. This file is a placeholder for Phase 1.
