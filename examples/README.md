# Examples

Runnable examples showing how to integrate `@company/payment-platform` into
real applications (Express, Fastify, NestJS, serverless, etc.).

No examples exist yet — they will be added once the public API is stable
enough to demonstrate. Each example will be a minimal, self-contained
project rather than a fragment, so it can be copy-pasted and run as-is.
